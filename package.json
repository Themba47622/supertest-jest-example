{
  "name": "supertest-jest-example",
  "version": "1.0.0",
  "description": "API test automation using supertest & jest",
  "main": "index.js",
  "scripts": {
    "test": "NODE_TLS_REJECT_UNAUTHORIZED=0 jest --reporters default jest-stare --coverage --detectOpenHandles --runInBand --testTimeout=60000",
    "test:watch": "jest --verbose --watchAll"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "jest": "^26.6.3",
    "jest-serial-runner": "^1.1.0",
    "jest-stare": "^2.2.0",
    "supertest": "^6.0.1"
  }
}
